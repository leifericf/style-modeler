.DS_Store

# Local outputs / intermediate artifacts
artefacts/**
!artefacts/.gitkeep

# Local raw sources (may contain personal writing)
sources/**
!sources/.gitkeep

# Local source manifest (may contain personal paths/URLs)
config/sources.yml

# Extra local manifests (keep examples tracked)
config/sources-*.yml
!config/sources-example.yml
!config/sources-smoke-example.yml

# Packaged profile bundles (may contain derived snippets)
bundles/**
!bundles/.gitkeep
